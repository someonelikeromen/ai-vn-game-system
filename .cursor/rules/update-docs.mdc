---
description: 每次修改代码后必须同步更新 ARCHITECTURE.md 和 CHANGELOG.md
alwaysApply: true
---

# 文档同步规范

每次对代码库进行实质性修改（新增功能、重构、Bug修复、配置变更）后，**必须**同步更新以下两个文件：

## ARCHITECTURE.md

- 若修改了文件结构、新增/删除模块、改变了数据流 → 更新对应章节
- 若修改了 API 路由（路径、参数、返回格式）→ 更新路由表
- 若修改了核心数据结构（statData、charCard、preset格式）→ 更新数据结构章节

## CHANGELOG.md

每次修改追加一个新版本块（版本号递增）：

```markdown
## [vX.Y] — 修改简述（YYYY-MM-DD）

### 修改文件
- `src/xxx/yyy.js`

### 修改内容
- 具体改了什么

### 修改原因
- 为什么改

### 修改结果
- 效果是什么
```

## 判断原则

- 仅修改注释、格式、日志文字 → 可以跳过
- 修改了逻辑、结构、接口、内容 → **必须更新**
- 本次会话中多次修改同一功能 → 可合并为一条记录
